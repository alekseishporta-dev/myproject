global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_config:
      - targets: ['localhost:9090']

  - job_name: 'node-exporter'
    static_config:
      - targets: ['dev-node-exporter:9100', 'prod-node-exporter:9100']